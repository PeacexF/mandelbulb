{
  "name": "mandelbulb",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.72",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
